package com.mincho.herb.global.response.error;

public enum HttpErrorType {
        BAD_REQUEST,
        UNAUTHORIZED,
        FORBIDDEN,
        CONFLICT,
        NOT_FOUND,
        METHOD_NOT_ALLOWED,
        INTERNAL_SERVER_ERROR;
}
