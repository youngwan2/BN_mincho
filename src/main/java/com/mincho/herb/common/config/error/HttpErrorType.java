package com.mincho.herb.common.config.error;

public enum HttpErrorType {
        BAD_REQUEST,
        UNAUTHORIZED,
        FORBIDDEN,
        NOT_FOUND,
        METHOD_NOT_ALLOWED,
        INTERNAL_SERVER_ERROR;
}
